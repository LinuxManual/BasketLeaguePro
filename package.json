{
  "name": "basketleaguepro",
  "version": "7.0.0",
  "private": true,
  "description": "BasketLeaguePro ultra dashboard with Firebase realtime sync and command center",
  "type": "commonjs",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "check": "node --check server.js && node --check script.js"
  }
}

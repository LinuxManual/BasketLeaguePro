{
  "name": "basketleaguepro",
  "version": "6.0.0",
  "private": true,
  "description": "BasketLeaguePro cloud-ready league dashboard with Firebase sync",
  "type": "commonjs",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "check": "node --check server.js && node --check script.js"
  }
}

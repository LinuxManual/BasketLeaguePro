{
  "name": "basketleaguepro",
  "version": "2.0.0",
  "private": true,
  "description": "BasketLeaguePro V2 with secure entry gate, admin-locked resets, and player ranking points system",
  "type": "commonjs",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "check": "node --check server.js && node --check script.js"
  }
}

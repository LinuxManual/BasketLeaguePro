{
  "name": "basketleaguepro",
  "version": "7.1.0",
  "private": true,
  "description": "BasketLeaguePro Basketball OS — score-only team games and separate 1v1 fixtures",
  "type": "commonjs",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "check": "node --check server.js"
  }
}
